# Experiment 9: Temporal Burstiness — SIGNIFICANT RESULT
#
# State media burstiness: B = 0.134 ± 0.194
# Clean burstiness: B = 0.019 ± 0.222
# Welch's t = 2.88, p < 0.005
#
# Combined filter (B > 0.3 AND categories >= 3):
# 6 candidates identified, ALL contain state media (100% precision)
#
# Burstiness parameter B = (CV - 1) / (CV + 1)
# where CV = std(inter-arrival gaps) / mean(inter-arrival gaps)
# B = -1: perfectly regular (automation)
# B = 0: Poisson random (organic)
# B = +1: maximally bursty (coordinated or breaking news)
#
# Key separation at B < 0.2:
# 55.8% state clusters vs 82.5% clean clusters fall below
#
# This is a content-free, language-independent behavioral signal.
